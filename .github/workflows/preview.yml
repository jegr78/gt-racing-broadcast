name: Preview
# Build a downloadable PRE-RELEASE binary for testing BEFORE a real release.
# Triggered by the 'preview' label on a PR (builds the PR branch) or by
# workflow_dispatch (builds a chosen ref, default main). Publishes a
# `preview-*` pre-release with public download URLs. preview-* tags never match
# v*, so release.yml and release-please are never triggered by a preview.
#
# SECURITY (review #8): the `labeled` trigger runs PR-HEAD code (checkout +
# build) with a write token (contents/pull-requests: write). This is safe ONLY
# because the repo is collaborator-gated: a fork PR cannot add a label, and the
# default `pull_request` token for fork PRs is read-only. The `preview` label is
# therefore a code-exec-with-write-token grant: it MUST only be addable by
# trusted maintainers. Do not loosen branch/label permissions for outside
# contributors, and review the diff before applying the label to any PR.
on:
  pull_request:
    types: [labeled, synchronize]
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch/ref to build a preview from"
        default: main

permissions:
  contents: write          # create/upload the pre-release
  pull-requests: write     # the download-links PR comment

# Per preview target (PR number, or the dispatched ref). The group must use
# parse-time expressions only -- it cannot reference the meta job's computed tag.
concurrency:
  group: preview-${{ github.event.pull_request.number || inputs.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  meta:
    # Only build for PRs that actually carry the 'preview' label: the labeled
    # event also fires for OTHER labels, and synchronize fires on every push.
    # workflow_dispatch is always allowed.
    if: >-
      github.event_name == 'workflow_dispatch' ||
      contains(github.event.pull_request.labels.*.name, 'preview')
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.id.outputs.tag }}
      version: ${{ steps.id.outputs.version }}
      title: ${{ steps.id.outputs.title }}
      sha: ${{ steps.id.outputs.sha }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          # PR: build the PR head commit. Dispatch: build the chosen ref.
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.ref || github.event.pull_request.head.sha }}
          fetch-depth: 0          # tags needed for the next-minor version fallback
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"
      - name: Compute preview identity (tag/version/title/sha)
        id: id
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          SHA="$(git rev-parse HEAD)"
          # The probable next release version embedded in the preview identity:
          # the open release-please PR's title wins; preview_meta.py falls back to
          # the next minor after the latest v* tag when no Release PR exists.
          RP_TITLE="$(gh pr list --state open --json title,headRefName \
            --jq 'map(select(.headRefName | startswith("release-please")))[0].title // ""' \
            2>/dev/null || true)"
          LATEST_TAG="$(git describe --tags --match 'v*' --abbrev=0 2>/dev/null || true)"
          python tools/preview_meta.py \
            --event "${{ github.event_name }}" \
            --pr "${{ github.event.pull_request.number }}" \
            --ref "${{ inputs.ref }}" \
            --sha "$SHA" \
            --release-pr-title "$RP_TITLE" \
            --latest-tag "$LATEST_TAG" >> "$GITHUB_OUTPUT"
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"

  release:
    needs: meta
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          ref: ${{ needs.meta.outputs.sha }}
          fetch-depth: 0          # full history so the changelog range resolves
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"
      - name: Build the changelog notes for this preview
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          # PR: the commits the PR adds (base..head). Dispatch: changes since the
          # last real release (a v* tag), else the most recent 30 commits.
          if [ "${{ github.event_name }}" = "pull_request" ] && [ -n "$BASE_SHA" ]; then
            RANGE="$BASE_SHA..HEAD"
          else
            PREV="$(git describe --tags --match 'v*' --abbrev=0 2>/dev/null || true)"
            [ -n "$PREV" ] && RANGE="$PREV..HEAD" || RANGE=""
          fi
          if [ -n "$RANGE" ] && git log --format=%s "$RANGE" > commits.txt 2>/dev/null; then
            :
          else
            git log --format=%s -n 30 > commits.txt
          fi
          python tools/preview_meta.py notes \
            --sha "${{ needs.meta.outputs.sha }}" < commits.txt > notes.md
      - name: (Re)create the rolling pre-release for this preview tag
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ needs.meta.outputs.tag }}
          SHA: ${{ needs.meta.outputs.sha }}
          TITLE: ${{ needs.meta.outputs.title }}
        run: |
          # Rolling: a preview tag is reused across builds. GitHub orders the
          # releases list by created_at = the *tagged commit's* date (not the
          # build/publish time), and `gh release create` IGNORES --target when the
          # tag already exists -- so a reused tag stays pinned to the commit it was
          # FIRST created on, freezing created_at at an old date and sinking the
          # preview among old releases. So move the tag to THIS build's commit
          # ourselves (force the ref), then recreate the release on it -- now
          # created_at tracks the fresh commit and the preview sorts to the top.
          gh release delete "$TAG" --yes || true
          if gh api "repos/$GH_REPO/git/refs/tags/$TAG" >/dev/null 2>&1; then
            gh api --method PATCH "repos/$GH_REPO/git/refs/tags/$TAG" \
              -f sha="$SHA" -F force=true >/dev/null      # re-point the existing tag
          else
            gh api --method POST "repos/$GH_REPO/git/refs" \
              -f ref="refs/tags/$TAG" -f sha="$SHA" >/dev/null   # first build: create it
          fi
          # The tag already points at $SHA, so --target is unnecessary here.
          gh release create "$TAG" \
            --prerelease \
            --title "$TITLE" \
            --notes-file notes.md

  build:
    needs: [meta, release]
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            asset: racecast-windows.zip
            built: dist/bin/racecast.exe
            binary: racecast.exe
            built_ui: dist/bin/racecast-ui.exe
            binary_ui: racecast-ui.exe
          - os: macos-latest
            asset: racecast-macos.tar.gz
            built: dist/bin/racecast
            binary: racecast
            built_ui: dist/bin/racecast-ui.app
            binary_ui: racecast-ui.app
          - os: ubuntu-latest
            asset: racecast-linux.tar.gz
            built: dist/bin/racecast
            binary: racecast
            built_ui: dist/bin/racecast-ui
            binary_ui: racecast-ui
          - os: ubuntu-24.04-arm        # GitHub-hosted ARM64 Linux runner
            asset: racecast-linux-arm64.tar.gz
            built: dist/bin/racecast
            binary: racecast
            built_ui: dist/bin/racecast-ui
            binary_ui: racecast-ui
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          ref: ${{ needs.meta.outputs.sha }}
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"
      - name: Run test suite (full gate: preview is green-built)
        run: python tools/run-tests.py
      - name: Install PyInstaller
        run: python -m pip install pyinstaller
      - name: Build + smoke-test the binary
        run: python tools/build-binary.py --version "${{ needs.meta.outputs.version }}"
      - name: Package the preview asset (binaries + .env.example)
        run: |
          mkdir staging
          cp -R "${{ matrix.built }}" "staging/${{ matrix.binary }}"
          cp -R "${{ matrix.built_ui }}" "staging/${{ matrix.binary_ui }}"
          cp .env.example staging/
          cd staging
          case "${{ matrix.asset }}" in
            *.zip)    python -m zipfile -c "../${{ matrix.asset }}" "${{ matrix.binary }}" "${{ matrix.binary_ui }}" .env.example ;;
            *.tar.gz) tar czf "../${{ matrix.asset }}" "${{ matrix.binary }}" "${{ matrix.binary_ui }}" .env.example ;;
            *)        echo "Unknown asset format: ${{ matrix.asset }}"; exit 1 ;;
          esac
      - name: Upload the asset to the pre-release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "${{ needs.meta.outputs.tag }}" "${{ matrix.asset }}" --clobber

  comment:
    needs: [meta, build]
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Post/refresh the download-links comment
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          PR: ${{ github.event.pull_request.number }}
          TAG: ${{ needs.meta.outputs.tag }}
          REPO: ${{ github.repository }}
          VERSION: ${{ needs.meta.outputs.version }}
        run: |
          BASE="https://github.com/$REPO/releases/download/$TAG"
          BODY="$(cat <<EOF
          <!-- preview-build -->
          ### 🔬 Preview build \`$VERSION\`

          Download (unsigned, one-time SmartScreen/Gatekeeper warning on first run):

          - **Windows:** $BASE/racecast-windows.zip
          - **macOS:** $BASE/racecast-macos.tar.gz
          - **Linux (x86-64):** $BASE/racecast-linux.tar.gz
          - **Linux (ARM64):** $BASE/racecast-linux-arm64.tar.gz

          Rebuilt automatically on every push to this PR. \`racecast --version\` prints \`$VERSION\`.
          EOF
          )"
          # --edit-last reuses the bot's previous preview comment; --create-if-none
          # posts the first one (gh >= 2.61, present on GitHub-hosted runners).
          gh pr comment "$PR" --body "$BODY" --edit-last --create-if-none
